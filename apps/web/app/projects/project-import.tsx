"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { apiUrl } from "@/lib/utils";
export default function ImportForm(){const [repoUrl,setRepoUrl]=useState("/workspace/examples/node-app"); const [busy,setBusy]=useState(false); const router=useRouter(); async function submit(){setBusy(true); const r=await fetch(`${apiUrl}/projects/import`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({repo_url:repoUrl})}); const p=await r.json(); setBusy(false); router.push(`/projects/${p.id}`)} return <div className="flex gap-2"><input className="w-80 rounded-xl border border-white/10 bg-white/5 px-3 text-sm" value={repoUrl} onChange={e=>setRepoUrl(e.target.value)} placeholder="GitHub URL or local path"/><Button disabled={busy} onClick={submit}>{busy?"Importing...":"Import repo"}</Button></div>}
